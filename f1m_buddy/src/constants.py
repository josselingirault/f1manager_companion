"""File with constants for the whole project"""

from pathlib import Path

PATH_SAVES = Path("/usr/home/project/saves")
PATH_BUDDY = PATH_SAVES / "buddy"
PATH_BUDDY_SAVES = PATH_BUDDY / "saves"
PATH_BUDDY_TEST = PATH_BUDDY / "test"
PATH_BUDDY_DATA = PATH_BUDDY / "data"
